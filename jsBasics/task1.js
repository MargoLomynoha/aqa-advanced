const row = 'Margo'; 
const numberValue = 9;
const booleanValue = true;
const nullValue = null;
const undefinedValue = undefined;

const logger = console.log;

logger(row);
logger(numberValue);
logger(booleanValue);
logger(nullValue);
logger(undefinedValue);
